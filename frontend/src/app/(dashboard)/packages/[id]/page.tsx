import { PackageDetailPage } from "@/components/shared/packages-detail";

export const metadata = { title: "Package Details | GYMMIS" };

export default function PackageDetailPageRoute() {
  return <PackageDetailPage />;
}